
<center>
	<h2> "<%= @phy.name %>"  Details</h2> 
	
	<table style="background-color:#A2ACBC">
		<tr><td>Name  </td><td>:</td><td> <%= @phy.name %></td></tr>
		<tr><td>Age   </td><td>:</td><td> <%= @phy.age %></td></tr>
		<tr><td>Experiance </td><td>:</td><td> <%= @phy.experiance %></td></tr>
		
	</table>
		<button><%= link_to "Back", physicians_path %> </button>


	<%= form_tag(physician_path ,method:'PATCH' ) do  %>
   		<%= text_field_tag(:search, params[:search]) %>
   		

  		<%= submit_tag("Add image", name: nil) %></p>

  		<span>
   				<% if $status == 1 %>
   					<p> image added sucessfully </p>
   				<% end %>	
   		</span>

	<% end %>

	<table>
		<tr><th>Comment</th><th>time</th></tr>
		<% @comments.each do |p| %>
			<tr><td><%= p.comment %></td><td><%= p.created_at %></td></tr>
		<% end %>
	</table>	
	

		
</center>
